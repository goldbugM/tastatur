{
  "private": true,
  "name": "@mkboard/database",
  "version": "0.0.0",
  "type": "module",
  "main": "lib/index.ts",
  "types": ".types/index.d.ts",
  "dependencies": {
    "@mkboard/config": "*",
    "@mkboard/names": "*",
    "@mkboard/oauth": "*",
    "@mkboard/pages-shared": "*",
    "@mkboard/publicid": "*",
    "@mkboard/rand": "*"
  },
  "devDependencies": {},
  "scripts": {
    "clean": "rm -fr .types",
    "compile": "tsc",
    "test": "tstest --import=@mkboard/test-env-server --test --test-concurrency=1 'lib/**/*.test.{ts,tsx}'",
    "fast-test": "env DATABASE_CLIENT=sqlite tstest --import=@mkboard/test-env-server --test 'lib/**/*.test.{ts,tsx}'"
  }
}
