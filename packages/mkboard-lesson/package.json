{
  "private": true,
  "name": "@mkboard/lesson",
  "version": "0.0.0",
  "type": "module",
  "main": "lib/index.ts",
  "types": ".types/index.d.ts",
  "dependencies": {
    "@mkboard/code": "*",
    "@mkboard/content": "*",
    "@mkboard/keyboard": "*",
    "@mkboard/lang": "*",
    "@mkboard/math": "*",
    "@mkboard/phonetic-model": "*",
    "@mkboard/rand": "*",
    "@mkboard/result": "*",
    "@mkboard/settings": "*",
    "@mkboard/textinput": "*",
    "@mkboard/unicode": "*"
  },
  "devDependencies": {},
  "scripts": {
    "clean": "rm -fr .types",
    "compile": "tsc",
    "test": "tstest --import=@mkboard/test-env-bundler --test 'lib/**/*.test.{ts,tsx}'"
  }
}
