{
  "private": true,
  "name": "@mkboard/lesson-ui",
  "version": "0.0.0",
  "type": "module",
  "main": "lib/index.ts",
  "types": ".types/index.d.ts",
  "dependencies": {
    "@mkboard/color": "*",
    "@mkboard/intl": "*",
    "@mkboard/lang": "*",
    "@mkboard/lesson": "*",
    "@mkboard/phonetic-model": "*",
    "@mkboard/result": "*",
    "@mkboard/settings": "*",
    "@mkboard/themes": "*",
    "@mkboard/widget": "*"
  },
  "devDependencies": {},
  "scripts": {
    "clean": "rm -fr .types",
    "compile": "tsc",
    "test": "tstest --import=@mkboard/test-env-browser --import=@mkboard/test-env-bundler --test 'lib/**/*.test.{ts,tsx}'"
  }
}
