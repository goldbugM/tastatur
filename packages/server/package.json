{
  "private": true,
  "name": "@mkboard/server",
  "version": "0.0.0",
  "type": "module",
  "dependencies": {
    "@mkboard/assets": "*",
    "@mkboard/config": "*",
    "@mkboard/database": "*",
    "@mkboard/highscores": "*",
    "@mkboard/intl": "*",
    "@mkboard/logger": "*",
    "@mkboard/multiplayer-server": "*",
    "@mkboard/multiplayer-shared": "*",
    "@mkboard/oauth": "*",
    "@mkboard/pages-server": "*",
    "@mkboard/pages-shared": "*",
    "@mkboard/publicid": "*",
    "@mkboard/result": "*",
    "@mkboard/result-io": "*",
    "@mkboard/result-userdata": "*",
    "@mkboard/settings": "*",
    "@mkboard/settings-database": "*",
    "@mkboard/themes": "*",
    "@paddle/paddle-node-sdk": "^3.0.0",
    "@types/ws": "^8.18.1",
    "ws": "^8.18.3"
  },
  "devDependencies": {},
  "scripts": {
    "clean": "rm -fr .types",
    "compile": "tsc",
    "test": "tstest --import=@mkboard/test-env-server --import=@mkboard/test-env-bundler --test --test-concurrency=1 'lib/**/*.test.{ts,tsx}'",
    "fast-test": "env DATABASE_CLIENT=sqlite tstest --import=@mkboard/test-env-server --import=@mkboard/test-env-bundler --test 'lib/**/*.test.{ts,tsx}'"
  }
}
