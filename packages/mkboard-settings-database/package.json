{
  "private": true,
  "name": "@mkboard/settings-database",
  "version": "0.0.0",
  "type": "module",
  "main": "lib/index.ts",
  "types": ".types/index.d.ts",
  "dependencies": {
    "@mkboard/config": "*",
    "@mkboard/settings": "*"
  },
  "devDependencies": {},
  "scripts": {
    "clean": "rm -fr .types",
    "compile": "tsc",
    "test": "tstest --import=@mkboard/test-env-server --test 'lib/**/*.test.{ts,tsx}'"
  }
}
